apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: '{{namespace}}'
  name: '{{deployment_name}}'
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: '{{app_name}}'
  replicas: 5
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: '{{app_name}}'
    spec:
      containers:
      - image: '{{image_name}}'
        imagePullPolicy: Always
        name: '{{app_name}}'
        resources:
           limits:
             memory: "128Mi"
             cpu: "500m"
           requests:
             memory: "64Mi"
             cpu: "250m"
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /readiness
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 1
          failureThreshold: 10
        livenessProbe:
          httpGet:
              path: /liveness
              port: 80
          initialDelaySeconds: 10
          periodSeconds: 30
          failureThreshold: 24
          timeoutSeconds: 1
